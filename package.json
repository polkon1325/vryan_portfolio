{
  "name": "a_portfolio_withhosting",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx @tailwindcss/cli -i ./src/input.css -o ./src/styles.css --watch",
    "build": "rm -rf ./dist && mkdir dist && npx tailwindcss -i ./src/input.css -o ./dist/styles.css --minify && cp ./src/index.html ./src/heroCanvas.js ./dist/ && cp -r ./src/assets ./dist/ && touch ./dist/.nojekyll",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@tailwindcss/cli": "^4.1.11",
    "tailwindcss": "^4.1.11"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.14"
    
  },
  "prettier": {
    "plugins": ["prettier-plugin-tailwindcss"]
  }
}
